## Desciption

    This project is a console app for network traffic generation. It consists of few main parts:
     - ping
     - traceroute
     - traffic generation
    
    And some helpfull utilities:
    - list all interfaces
    - list all netns
    - create netns

## Build
    App can be build by cmake

    ```bash

    ```

## Usage
    First of all use help command to get some helpfull tips for using app:

    ```bash
        trgen --help
    ```

## TODO
    
    - ping implemetation
    - traceroute implementation
    - traffic generator implementation
    - list interface
    - cli autocomplete vtush like
    - adding vlan id
    - netns create
    - save in PCAP files
    - dynamic plugin support openswitch like
